//this is a dummy file so that GitHub identifies this repo as using cuda language.
// we generate _cu from headers via cmake in the project, so github assumes it is only cpp,


int main (){
 
return 0;

}